<Window x:Class="PassNotes.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PassNotes"
        Title="{Binding Source={x:Static local:Loc.Instance}, Path=[AppTitle]}"
        Width="400"
        MinWidth="400"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Style="{StaticResource BaselineDialogWindow}">
    <Grid Margin="16">
        <Border Style="{StaticResource BaselineDialogSurfaceBorder}"
                Padding="16">
            <local:LoginEditorView x:Name="LoginView"/>
        </Border>
    </Grid>
</Window>
