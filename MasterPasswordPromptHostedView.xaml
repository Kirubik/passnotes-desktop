<UserControl x:Class="PassNotes.MasterPasswordPromptHostedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PassNotes">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptTextBlock"
                   Grid.Row="0"
                   TextWrapping="Wrap"/>

        <PasswordBox x:Name="Pwd"
                     Grid.Row="2"
                     MinWidth="320"
                     PasswordChanged="Pwd_PasswordChanged"
                     ContextMenu="{StaticResource BaselineTextCommandsContextMenu}"/>

        <TextBlock x:Name="ErrorTextBlock"
                   Grid.Row="4"
                   Foreground="{DynamicResource Brush.TextPrimary}"
                   MinHeight="20"
                   TextWrapping="Wrap"
                   Visibility="Hidden"/>
    </Grid>
</UserControl>
